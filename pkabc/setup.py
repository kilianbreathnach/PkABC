from setuptools import setup

setup(name='pkabc',
      version='0.1',
      description='generate analytic galaxy powerspectrum',
      author='The cosmo boys',
      license='MIT',
      packages=['pkabc'],
      zip_safe=False)
